{% extends 'base.html.twig' %}

{% block title %}PanelSolar{% endblock %}

{% block body %}
    <h1>PanelSolar</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ panel_solar.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('panel_solar_index') }}">back to list</a>

    <a href="{{ path('panel_solar_edit', {'id': panel_solar.id}) }}">edit</a>

    {{ include('panel_solar/_delete_form.html.twig') }}
{% endblock %}
