{% extends 'base.html.twig' %}

{% block title %}ServicioLuz{% endblock %}

{% block body %}
    <h1>ServicioLuz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ servicio_luz.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('servicio_luz_index') }}">back to list</a>

    <a href="{{ path('servicio_luz_edit', {'id': servicio_luz.id}) }}">edit</a>

    {{ include('servicio_luz/_delete_form.html.twig') }}
{% endblock %}
