{% extends 'base.html.twig' %}

{% block title %}ServicioLuz index{% endblock %}

{% block body %}
    <h1>ServicioLuz index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for servicio_luz in servicio_luzs %}
            <tr>
                <td>{{ servicio_luz.id }}</td>
                <td>
                    <a href="{{ path('servicio_luz_show', {'id': servicio_luz.id}) }}">show</a>
                    <a href="{{ path('servicio_luz_edit', {'id': servicio_luz.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('servicio_luz_new') }}">Create new</a>
{% endblock %}
