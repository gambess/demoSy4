<form method="post" action="{{ path('caseta_sanitaria_delete', {'id': caseta_sanitarium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caseta_sanitarium.id) }}">
    <button class="btn">Delete</button>
</form>
