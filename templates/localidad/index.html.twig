{% extends 'base.html.twig' %}

{% block title %}Localidad index{% endblock %}

{% block body %}
    <h1>Localidad index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Codigo</th>
                <th>Nombre</th>
                <th>Altura</th>
                <th>Personas</th>
                <th>Casas</th>
                <th>Pararrayos</th>
                <th>Prioridad</th>
                <th>AlumbradoPublico</th>
                <th>Motivo</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for localidad in localidads %}
            <tr>
                <td>{{ localidad.id }}</td>
                <td>{{ localidad.codigo }}</td>
                <td>{{ localidad.nombre }}</td>
                <td>{{ localidad.altura }}</td>
                <td>{{ localidad.personas }}</td>
                <td>{{ localidad.casas }}</td>
                <td>{{ localidad.pararrayos }}</td>
                <td>{{ localidad.prioridad }}</td>
                <td>{{ localidad.alumbradoPublico }}</td>
                <td>{{ localidad.motivo }}</td>
                <td>{{ localidad.descripcion }}</td>
                <td>
                    <a href="{{ path('localidad_show', {'id': localidad.id}) }}">show</a>
                    <a href="{{ path('localidad_edit', {'id': localidad.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="12">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('localidad_new') }}">Create new</a>
{% endblock %}
