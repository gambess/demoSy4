{% extends 'base.html.twig' %}

{% block title %}ServicioAgua index{% endblock %}

{% block body %}
    <h1>ServicioAgua index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for servicio_agua in servicio_aguas %}
            <tr>
                <td>{{ servicio_agua.id }}</td>
                <td>
                    <a href="{{ path('servicio_agua_show', {'id': servicio_agua.id}) }}">show</a>
                    <a href="{{ path('servicio_agua_edit', {'id': servicio_agua.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('servicio_agua_new') }}">Create new</a>
{% endblock %}
