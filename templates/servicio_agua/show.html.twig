{% extends 'base.html.twig' %}

{% block title %}ServicioAgua{% endblock %}

{% block body %}
    <h1>ServicioAgua</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ servicio_agua.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('servicio_agua_index') }}">back to list</a>

    <a href="{{ path('servicio_agua_edit', {'id': servicio_agua.id}) }}">edit</a>

    {{ include('servicio_agua/_delete_form.html.twig') }}
{% endblock %}
