{% extends 'base.html.twig' %}

{% block title %}Propietario{% endblock %}

{% block body %}
    <h1>Propietario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ propietario.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ propietario.nombre }}</td>
            </tr>
            <tr>
                <th>Apellido1</th>
                <td>{{ propietario.apellido1 }}</td>
            </tr>
            <tr>
                <th>Apellido2</th>
                <td>{{ propietario.apellido2 }}</td>
            </tr>
            <tr>
                <th>TipoDocumento</th>
                <td>{{ propietario.tipoDocumento }}</td>
            </tr>
            <tr>
                <th>NumeroDocumento</th>
                <td>{{ propietario.numeroDocumento }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('propietario_index') }}">back to list</a>

    <a href="{{ path('propietario_edit', {'id': propietario.id}) }}">edit</a>

    {{ include('propietario/_delete_form.html.twig') }}
{% endblock %}
