{% extends 'base.html.twig' %}

{% block title %}Comuna{% endblock %}

{% block body %}
    <h1>Comuna</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comuna.id }}</td>
            </tr>
            <tr>
                <th>Codigo</th>
                <td>{{ comuna.codigo }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ comuna.nombre }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('comuna_index') }}">back to list</a>

    <a href="{{ path('comuna_edit', {'id': comuna.id}) }}">edit</a>

    {{ include('comuna/_delete_form.html.twig') }}
{% endblock %}
